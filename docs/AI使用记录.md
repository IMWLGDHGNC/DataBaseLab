# AI 使用记录

课程允许 AI 辅助，但要求保留提示词、候选答案、人工修改和验证证据；AI 输出不能替代个人理解、SQL 测试与答辩。本文件如实记录已有材料，不补造历史对话或人工确认。

## 2026-09-11：完善第一周业务分析

### 用户提示词

> 为了更好的做这个长期实验，现在week1.md可以了吗，有什么要完善的可以完善，也可以补充下mermaid图更直观，但所有修改都要符合规则，完成后提交并push

### 输入与候选输出

- 工具：Codex。
- 输入材料：[第一周任务讲解](第一周任务讲解.md)、修改前的 `docs/Week1.md`、README、仓库状态与 Git 历史。
- 修改前基线：Git 提交 `c11155b`；可用 `git show c11155b:docs/Week1.md` 查看当时正文。
- 本轮候选答案：与本记录同次提交的 [Week1.md](Week1.md) 完整修订稿。其正文是 AI 生成并自查后的需求草稿，小组尚未完成人工复核；可用 `git log --oneline -- docs/Week1.md` 找到本次提交，再用 `git show <本次提交号> -- docs/Week1.md` 查看逐项修改。当前没有另行提供给小组选择的候选版本。

### AI 提出的主要修改与理由

| 原问题 | 本轮建议 |
| --- | --- |
| 第一阶段提到积分使用、会员优惠与售后，但没有相应流程 | 统一为只累计积分；兑换、优惠、退款与退货列入暂缓范围 |
| 原文允许完成前取消，可能包含已经支付的订单 | 仅允许待支付取消；已支付无法履约先登记异常，明确退款扩展边界 |
| 流程提到锁定库存，但核心对象没有锁定信息 | 增补批次锁定记录，区分现存量、锁定量、可用量及过期批次 |
| 缺少可复核的流程状态、业务样例和长期衔接 | 增加三张 Mermaid 图、规则编号、需求推演以及四阶段衔接 |
| 小组成员与人工确认信息缺失 | 保留待填写项，不把经营角色当作组员，不声称已人工确认 |

### 验证证据

- 内容对照：按课程第一周要求核查场景、角色、流程和数据边界；未新增 SQL、表结构或实际应用实现，课程讲解原文保持不变。
- 业务走查：`Week1.md` 第 8 节列出正常采购与销售、取消、缺货、过期、重复操作、履约异常和未来失败回滚的预期结果。这是需求推演，尚无数据库可运行测试。
- Markdown 检查：4 份文档的 UTF-8 编码、代码块闭合、表格列数和 7 个本地文件链接均通过；确认业务分析包含 3 个 Mermaid 代码块、11 条编号规则，库存与积分示例算术一致。
- Mermaid 检查：使用 Mermaid CLI `11.17.0` 与本机 Chrome 将 3 张图全部渲染为 PNG，命令退出码为 0；逐张查看，中文标签、分支与状态连线可读，无文字裁切。检查时缩短了销售图的两个判断标签，详细含义仍保留在正文。
- 渲染复现：在仓库根目录使用 Mermaid CLI 的 `mmdc -i docs/Week1.md -o <临时目录>/Week1.md -p <浏览器配置路径> -e png`；浏览器配置的 `executablePath` 指向本机 Chrome。预览产物放在系统临时目录 `databaselab-week1-review`，不作为项目实现文件提交。
- Git 检查：`git diff --check` 通过，课程讲解文件无差异；改动范围仅为业务分析、README 索引与本 AI 记录。Windows 的换行转换提示不属于内容错误。

### 人工修改与复核

尚未收到小组成员对本轮正文的实际修改或确认。文档中的单仓、整单处理、模拟支付、15 分钟有效期、到期日禁售、积分累计和暂停履约规则均为本轮建议，需成员理解并复核。

| 复核人 | 实际修改或接受理由 | 验证依据 | 状态 |
| --- | --- | --- | --- |
| 待填写 | 待填写实际意见，不以 AI 自查代替人工复核 | 待填写讨论或验证记录 | 待复核 |

后续每轮 AI 协助应追加真实提示词、对应输出或提交引用、人工修改及验证证据。已有文件在本轮之前是否使用 AI、使用过哪些提示词尚无可核实记录，不在此推断。

## 2026-09-16：补全第二周字段定义、码与样例

### 用户提示词

> 你先把每个表每个字段写清：字段名、类型、长度/精度、是否允许空、默认值、域（取值范围/枚举）、字段的含义；再1. 每张表标出主码（PK）、候选码（UNIQUE 候选）、外码（FK → 哪个表哪个字段），再补上2-3行真实数据

### 输入与 AI 输出

- 工具：Codex。
- 输入材料：`docs/Week1.md` 的业务边界、`docs/Week2.md` 的现有表和字段、`docs/第二周任务讲解.md` 的课程要求，以及本轮修改前的 `Week2.md` 备份。
- AI 输出：本轮修改后的 [Week2.md](Week2.md)。对现有八个表或列表补充字段类型、长度或精度、空值、默认值、域、含义、PK、其他 UNIQUE 候选与 FK，并为每个对象补充 2–3 行样例。
- 样例是贴近零食网店业务的**虚构数据**，不是实际交易或实际个人资料；用户原文中的“真实数据”按“符合真实业务规律的实例”处理，未冒用真实顾客信息。
- 沿用原草稿字段，没有把上一轮评审提出的拆表方案直接改进设计。退货和个人订单列表仍保留，但明确其与第一周范围及实际存储方式的冲突。

### 验证与人工复核

- Markdown 表格列数检查通过；八个对象均有字段字典和 2–3 行样例。
- 人工核算样例：`S00` 期初 30＋入库 500－出库 10＝现存 520；`S01` 期初 60＋入库 300－出库 10＝现存 350；`ST00` 10×4.40＝44.00，`ST01` 10×2.00＝20.00。
- FK 样例编号检查：商品 `S00/S01`、员工 `P00/P01`、顾客 `C00/C01`、出库 `OT00/OT01` 和销售单 `ST00/ST01` 均能在相应样例表找到。
- 尚未建库或运行 SQL；样例的约束有效性、权限与事务正确性未做数据库测试。
- 小组成员对表结构、候选码业务条件和样例的人工复核尚待完成，不将 AI 自查写成组员已确认。

## 2026-09-17：调整第二周关系模式

### 用户提示词

> 你的新版打算增加什么表。

> 增加、调整这些表

### 输入与 AI 输出

- 工具：Codex。
- 输入材料：[第一周业务分析](Week1.md)、调整前的 [第二周关系模式草稿](Week2.md)、第二周任务讲解，以及之前关于新增表、调整入库与出库表的讨论。调整前版本另存于本机临时备份，仓库中未将其伪称为已提交版本。
- AI 输出：本轮修订后的 [Week2.md](Week2.md)。由原有八个表或列表草稿调整为 15 张实体表；新增商品类别、供应商、采购明细、库存批次、库存锁定、销售明细、支付记录、积分变动和订单异常处理；将入库订单改为采购订单主表，将出库订单改为库存变动；暂缓退货，个人订单列表不重复存表。
- 所有样例均为符合业务规则的虚构测试数据，不代表真实交易或真实个人资料。为展示一张销售单含多商品及完整库存流水，部分表的样例超过课程讲解建议的 2–3 行。

### 验证与人工复核

- Markdown 结构检查：15 张实体表均有完整字段字典、码说明和至少 2 行样例，所有表格列数一致，标题与代码围栏检查通过。
- 跨表样例核对：类别、供应商、员工、顾客、采购单及明细、批次、销售单及明细、锁定、库存变动、支付、积分、异常记录的引用编号均能在样例中找到；批次对应的采购商品与锁定或出库的销售商品一致。
- 算术与时间核对：采购货款、订单总额、成功支付金额、15 分钟支付有效期、批次现存量、锁定量和顾客积分余额一致。`ST02` 已支付但因批次到期维持待出库，没有出库流水或赠分。
- 尚未运行 SQL Server 建库、约束、权限或并发事务测试。小组成员仍需人工确认组合候选码、经营角色、异常处置和课程要求，AI 自查不等于人工验收。

## 2026-09-17：补充店铺运营流程图

### 用户提示词

> 你画一个图，图中解释一下这家店的运营逻辑，在图上标出每一个环节对应的表。现在这样15个表没有逻辑地摆在那里。

### 输入与 AI 输出

- 工具：Codex。
- 输入材料：本轮调整后的 [Week2.md](Week2.md) 15 表关系模式、[Week1.md](Week1.md) 的采购与销售流程和业务边界。
- AI 输出：在 `Week2.md` 的字段定义之前加入 Mermaid 运营流程图，按补货、采购审批、验收入库、下单、整单锁定、模拟支付、出库复核、异常暂停和会员赠分连接业务环节，并在节点中标出参与的表。
- 图中的箭头表示业务流程，不代表外码；图中不包含第一阶段暂缓的退货和退款。

### 验证与人工复核

- 检查图中覆盖全部 15 张实体表，且订单创建与整单锁定同时成功或失败、支付成功不等于交易完成、库存变动与库存锁定分离、批次异常不虚记出库或积分。
- 检查 Mermaid 代码围栏闭合、节点名称与连线目标一致；原有 15 表字段与样例未因插图改变。
- 本图是业务说明，不是已经运行的 SQL 或事务测试。小组仍需按实际经营假设复核流程。

## 2026-09-17：修复第二周评审问题

### 用户提示词

> 拉取最新内容，review

> 修复

### 输入与 AI 输出

- 工具：Codex。
- 输入材料：提交 `64507be` 的第二周关系模式、第一周业务规则，以及本轮评审发现的两处约束缺漏。
- AI 输出：修订 [Week2.md](Week2.md) 的赠分流程与字段域，明确会员实付不足 1 元时正常完成订单、不产生积分流水、不更新余额；补充采购入库流水与批次必须引用同一采购明细的跨表规则。

### 验证与人工复核

- 文档边界走查：会员实付 0.50 元时走不赠分分支；实付 1.00 元时可记录 1 分，与第一周 R07 一致。
- 来源走查：入库样例 `IM00/IM01/IM02` 的采购明细分别与 `B00/B01/B02` 一致；同商品但不同采购明细的交叉引用被新增规则排除。
- 本轮为文档修复，尚未运行 SQL Server 或验证数据库约束；人工复核仍待小组完成。

## 2026-09-17：准备本地 SQL 环境

### 用户提示词

> 可以运行sql语句

> 你帮我配置好本地环境

### 输出与验证状态

- 检查本机未发现 SQL Server 实例或 LocalDB 工具；发现已停止的 MySQL80 服务。
- 准备 SQL Server 2022 LocalDB 安装脚本、当前用户实例初始化脚本、PowerShell SQL 执行器和环境验证 SQL，并补充本地环境说明。
- 自动安装操作被工具策略阻止，尚未安装成功或实际运行 SQL。验证脚本只测试环境及简单金额计算，不代表第二周业务模式已实现或验证。

### 用户安装后的实机验证

- 用户回复 `ok` 后重新检查，确认 SQL Server 2022 LocalDB `16.0.1000.6` 已安装。
- 运行初始化脚本创建并启动 `DataBaseLab` 实例；首次连接因 PowerShell 将 `DataSource` 属性解析为连接字符串键而失败，改用 `Data Source` 等标准键后重新执行成功。
- 实际创建 `DataBaseLab` 数据库，执行临时表建表、插入、查询、事务回滚，环境验证返回 `PASS`。金额 0.50、1.00 的积分计算分别为 0、1，生成流水标志分别为 0、1。
- 以上是实际 SQL 环境验证；15 张业务表、跨表约束与完整业务事务尚未实现或验证。

### 用户终端连接问题

- 用户原样执行示意文件名“你的脚本.sql”，报错“指定的 LocalDB 实例不存在”。工具端检查仍能看到当前账户的 `DataBaseLab` 实例；尚未确认用户终端与工具端可见实例不同的原因，不能把工具端成功等同于用户终端成功。
- 修正执行器的检查顺序：先验证 SQL 文件存在，再检查 LocalDB 安装和当前用户可见实例，启动已有实例；缺少实例时提供同一终端运行初始化脚本的具体命令。
- 补充用户终端排障说明，使用真实存在的环境验证 SQL，避免要求用户直接执行占位文件名。

### 数据库物理文件冲突修复

- 用户在自己的终端创建实例成功，但创建数据库时发现 `C:\Users\18456\DataBaseLab.mdf` 已存在。原脚本只判断当前实例是否已有数据库，未避开其他实例或遗留物理文件。
- 修改环境 SQL：新建数据库时从当前实例的 `master` 文件位置取得目录，显式指定带唯一后缀的 MDF/LDF 路径；已注册数据库继续复用，原有文件保持不变。验证输出增加实际数据文件位置。
- 实机执行修复后的初始化成功，数据库文件位于 `Instances\DataBaseLab` 目录，验证返回 `PASS`；再次初始化仍返回 `PASS`，复用已有数据库。执行器改为换行显示长字段，以完整展示数据库文件路径。

## 2026-09-17：人工复核确认与提交

### 用户提示词

> 人工复核完成，提交并push

### 复核记录

- 用户明确确认本轮人工复核完成，授权提交并推送第二周修复及本地 SQL 环境相关改动。
- README 与第二周文档已同步复核状态；此前各轮“待复核”描述保留为当时的历史记录。
- 用户未提供成员姓名、具体分工或逐项修改意见，不代填这些信息。业务表及其跨表约束仍需第三周实现，环境验证成功不代表业务实现已完成。
