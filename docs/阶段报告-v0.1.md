# 线上零食网店数据库原型 v0.1 阶段报告

## 设计思路

项目把线上零食网店的采购、批次库存、销售订单、模拟支付和会员积分纳入数据库；退货、退款、真实支付和自动配送暂不纳入。业务边界与店长、采购员、库存管理员、订单管理员职责见 [Week1](Week1.md)。

关系模式由 15 张表构成。商品与类别分开，库存按采购来源建批次，订单明细保存成交时单价，库存锁定与现存量变动分别记账。主码采用稳定业务编号；商品组合候选码、类别名称与顾客账户等使用 UNIQUE；外码连接采购、批次、订单、支付、积分与经办人。完整字段、码和样例见 [Week2](Week2.md)。

## 实验过程

第三周使用 SQL Server 2022 LocalDB 实现建库、建表、46 行样例数据和商品、库存、订单 CRUD，并验证 12 项非法数据。第四周增加多表连接、已完成销售统计、库存状态查询、三个视图、异常解决说明约束和四类角色的列级/视图级授权。全流程用 [Run-Week4.ps1](../scripts/Run-Week4.ps1) 从空库执行；具体顺序和运行命令见 [Week4](Week4.md)。

第一次在 `DataBaseLab_Week4` 运行，第二次在独立新库运行。两次均得到 `WEEK4_PASS`，且最终为 15 表、46 行。完成销售额核对为 64.50 元、15 件；B01 在演示日期已到期，可售量为 0。新约束六例、原有 12 项非法数据测试和权限 13 例均通过。完整查询和测试输出保存在 [result 目录](../result/README.md)。

## 实验总结

把“已支付”直接计为“已销售”会多算待出库订单；本阶段统计以“已完成”为口径。视图便于重复查询，但当前库存视图按当天判断是否到期，历史报表须指定明确的截至日期。CHECK、FK 和唯一性约束可直接拒绝多类非法写入；涉及多表金额、库存流水、角色审批人与业务岗位一致性等规则仍需要受控业务入口和事务。角色测试证明了当前授予范围内的允许与拒绝，不代表已经实现所有岗位的完整业务工作流。

AI 参与生成候选 SQL、说明及测试脚本，实际运行结果已记录在 [AI 使用记录](AI使用记录.md)。组员分工与逐句人工复核意见目前未填写，不能据此宣称人工验收完成。
